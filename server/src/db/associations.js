//const Users = require('../models/User')
//const CreditCard = require('../models/CreditCard')
//const Products = require('../models/Products')
//const Orders = require('../models/Orders')
//const Brands = require('../models/Brands')

//const Product = require('../models/Product')
/*
const relacionaUserProduct = () => {
    User.hasMany(Product, { foreignKey: 'productId', sourceKey: 'id'})
    Product.belongsTo(User, { foreignKey: 'productId', targetId: 'id'})
}
*/
// Relacion 1 a 1
// Añade una clave ajena creditcardId a la tabla users;
//CreditCard.hasOne(Users) 
//Users.belongsTo(CreditCard)
